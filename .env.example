# ADIZ 圖片座標抽取管線 - 環境變數範例
# 複製為 .env 並填入實際值

# 圖片目錄（預設 adiz_images）
ADIZ_IMAGES_DIR=adiz_images

# Gemini API（OCR 必填）
GEMINI_API_KEY=your_gemini_api_key_here

# 可選：Gemini 模型
GEMINI_MODEL=gemini-1.5-flash

# BigQuery（入庫必填）
BQ_PROJECT=your_gcp_project_id
BQ_DATASET=adiz
